__match_y = \self. \val. \((pat, expr), rest).
  if zip_env (pat, val) != nil then zip_env (pat, val) :: expr
  else if rest != nil          then self self val rest
  else nil
;

op nonassoc 3.7 (a -> b) = a, b;
op left 3 (a ?> b) = __match_y __match_y a b;

